# SPEC-SYS-1109202601 — BC Motor: Tab Configuración

**Documento:** Tasks · **Fase:** ✅ Grupos F-K (filtros fijos) implementados y verificados en dev (2026-09-24); falta la confirmación visual del usuario. Grupos A-E: ✅ documentación retroactiva verificada —
no hay código para escribir (spec 100% "ya construido", a pedido
explícito: "NO PROGRAMAR NADA"). Cada tarea es una verificación
punto-por-punto de `requirements.md` contra el código real citado en
`design.md`, no una tarea de implementación.

## Grupo A — Encabezado ✅

1. [x] R1/R2 verificados contra `EncabezadoBcComponents` +
   `handle_event("guardar_header", ...)` en `bc_motor_live.ex:208` —
   mismo componente que usa el asistente de alta, mismo
   `MetaSchemaContext.actualizar_header/2` de siempre.

## Grupo B — Campos ✅

2. [x] R3 (grid de campos) verificado contra `@campos =
   MetaSchemaContext.listar_detalles/1` + la tabla de
   `bc_motor_live.ex:2367-2464`, columna por columna.
3. [x] R4 (drag-and-drop) verificado: hook `ListaOrdenable`
   (`assets/js/app.js:875`, Sortable.js) → evento único `mover_a` →
   `handle_event("mover_a", ...)` (`bc_motor_live.ex:851`) →
   `MetaSchemaContext.reordenar_campos/2` (solo metadata, sin ALTER).
4. [x] R5/R6 (Formato captura/fecha) verificados contra los botones
   condicionados por tipo, `bc_motor_live.ex:2430-2440`.
5. [x] R7 (Configurar + Cascada para referencia) verificado — dos
   botones independientes, `bc_motor_live.ex:2441-2453`, y su
   contraparte en el tab Relaciones (`panel_relaciones/1`, mismos
   `handle_event`).
6. [x] R8 (Eliminar campo) verificado contra
   `CatalogoGenerador.eliminar_campo/4` — confirmación escrita
   (`validar_confirmacion/2`), `DROP COLUMN` real
   (`quitar_columna/2`), auditoría (`MetaAuditoriaDefinicion.registrar/4`).
   **Corrección real durante la verificación**: el primer borrador de
   `requirements.md` decía "pide confirmación" en genérico — el
   mecanismo real exige escribir el nombre del campo, más fuerte que un
   diálogo sí/no. Corregido en `requirements.md` antes de seguir.
7. [x] R9 (Agregar campo) verificado contra
   `handle_event("guardar_campo_asistente", ...)` →
   `FieldDesignerComponents.construir_propiedades/3` →
   `guardar_campo_y_generar/4` → `CatalogoGenerador.generar/1`
   (rama retrofit, reusa el generador de catálogo nuevo) — "Valor por
   default" ya documentado en `catalogo-maestro-detalle-requerimientos.md` R13.
   **Corrección real durante la verificación**: `design.md` primero
   afirmó una función `CatalogoGenerador.agregar_campo/2` que no
   existe — corregido con el nombre real (`generar/1`) tras grep
   directo.

## Grupo C — Estados ✅

8. [x] R10 (gate por campos) verificado: `@puede_agregar` de
   `tabla_estados/1` = `@campos != []`.
9. [x] R11/R12 (estado inicial forzado) verificado contra
   `MetaEstadosAdmin.crear_estado/1` →
   `forzar_inicial_si_es_el_primero/1` (`meta_estados_admin.ex:58`) +
   `modal_estado/1` (`bc_motor_live.ex:4302`).
10. [x] R13 (eliminar estado) verificado: botón condicionado a
    `not MapSet.member?(@referenciados, estado.id)` en
    `tabla_estados/1`.

## Grupo D — Transiciones ✅

11. [x] R14 (gate por estado inicial) verificado: `@puede_agregar` de
    `tabla_transiciones/1` = existe algún `es_inicial: true`.
12. [x] R15/R16 (tabla + aviso self-loop) verificado contra
    `tabla_transiciones/1`, `bc_motor_live.ex:2935-3007`.
13. [x] R17 (self-loop `"guardar"` como convención de PATCH)
    verificado contra `MetaStateEngine.transicion_guardar/2`
    (`meta_state_engine.ex:178-190`) — búsqueda exacta por
    `accion == "guardar"` + mismo origen/destino.
    **Corrección real durante la verificación**: `design.md` primero
    atribuyó esto a `MetaTransicionController`/`CatalogoGenerico.actualizar/2`
    sin haberlo verificado — corregido con la función real tras grep.
14. [x] R18 (form Origen/Destino) verificado contra `modal_transicion/1`.
15. [x] R19 (tabs de campos editables por catálogo detalle) verificado
    contra `modal_transicion/1` + `.grupo_campos_editables/1`,
    `@catalogos_detalle` desde `MetaSchemaContext.listar_catalogos_detalle/1`.
16. [x] R20/R21 (permisos por transición) verificado contra
    `permisos_transicion/3` (`bc_motor_live.ex:180-186`) +
    `handle_event("registrar_permiso_transicion", ...)`
    (`bc_motor_live.ex:1389-1401`).

## Grupo E — Stepper de completitud ✅

17. [x] R22/R23 verificados contra `pasos_motor/5`
    (`bc_motor_live.ex:2225-2277`) y
    `MetaEstadosAdmin.completitud/1` (`meta_estados_admin.ex:479`).

---

**Filtros fijos en campos referencia (R24-R30, `design.md` §3.6)**:
a diferencia de los grupos A-E (verificación retroactiva), estos son
grupos de **implementación**, aprobados el 2026-09-24.

## Grupo F — Filtro en tiempo de ejecución (R25-R26) ✅

18. [x] `CatalogoGenerico.aplicar_filtro/3`: nueva cláusula
    `{:en_ci, valores}` → `upper(trim(CAST(col AS text))) IN ^valores` (el CAST, agregado al implementar, deja filtrar también columnas numéricas o enums: `trim` sobre un entero falla en Postgres).
19. [x] `CatalogoGenerico.opciones_referencia/3`: suma
    `props["filtros_fijos"]` a los filtros de `dependencias` como lista
    de tuplas, en las dos ramas (sistema y BPB).
20. [x] Tests: filtro solo (Y entre filtros, O entre valores,
    mayúsculas/espacios, `nil` excluido), combinado con una
    dependencia sobre otra columna y con el acotamiento por sucursal.

## Grupo G — Validación (R27-R28) ✅

21. [x] `MetaSchemaContext.validar_filtros_fijos/2`: descarta filas
    vacías, rechaza campo inexistente o valores vacíos, normaliza
    (`trim` + `upcase`).
22. [x] `validar_dependencias_referencia/2`: incluye los campos con
    `filtros_fijos`; valida solo si hay `get_change/2`; resuelve el
    destino con `CatalogoGenerico.modulo_destino_de/1`; ignora
    metadata mal formada sin tronar.
23. [x] Tests: rechaza un valor fuera del filtro al crear y al cambiar;
    no valida si el campo no cambió; no truena con una columna
    inexistente en la configuración.

## Grupo H — Configuración en BcMotorLive (R7, R24, R28) ✅

24. [x] `abrir_form_dependencia`: `campos_seleccionables_de/1` y
    etiqueta de `catalogo_sistema/1` (arregla el error con destinos de
    sistema).
25. [x] Botón "Cascada" → "Filtros" (fila de Campos y panel
    Relaciones), con "✓" si hay `dependencias` o `filtros_fijos`.
26. [x] `modal_dependencia/1`: sección "Filtros fijos" (select de
    campo, input de valores separados por coma, `<datalist>` con hasta
    50 valores distintos reales, agregar/quitar fila).
27. [x] `guardar_dependencia`: valida con `validar_filtros_fijos/2` y
    guarda `dependencias` + `filtros_fijos` en la misma llamada.
28. [x] Tests LiveView del modal (abrir con destino de sistema, agregar
    filtro, guardar, error de validación).

## Grupo I — Publicación (R29-R30) ✅

29. [x] `MetaImportExport`: `sincronizar_propiedades_referencia_campos/2`
    con `@propiedades_referencia ~w(campo_visualizacion
    campos_acompanamiento campos_relacion dependencias filtros_fijos mensaje_sin_padre)`: aplica las que
    cambiaron y quita las que el origen ya no trae.
30. [x] Tests: agregar, cambiar y quitar cada clave en un campo ya
    existente.

## Grupo J — Texto de producto con voseo ✅

31. [x] "Escribí" → "Escribe" (`campo_input_components.ex`,
    `referencia_field.js`); "agregá"/"definí" → "agrega"/"define"
    (`bc_motor_live.ex`, `bc_nuevo_completo_live.ex`). Encontrado de paso: "Elegí un ambiente…" → "Elige" (`endpoints_live.ex`), `propagacion_live_test.exs` que seguía buscando "Elegí un ambiente" (la pantalla ya se había corregido en `b24f5c8`, el test no), y `docs/campos-filtros-en-cascada.md` reescrita (voseo, botón "Filtros", sección de filtros fijos).

## Grupo K — Verificación real y cierre ✅ (falta confirmación visual)

32. [x] Configurar en dev `pty_dsd_repartos` → Almacén:
    `inventory_type = COMPROMETIDA`. Verificado contra Postgres de dev
    (2026-09-24) por el mismo camino que usa el formulario
    (`validar_filtros_fijos/2` + `actualizar_detalle/2`, quedó guardado
    `[%{"campo" => "inventory_type", "valores" => ["COMPROMETIDA"]}]`):
    de 3 almacenes (id 1 y 2 "ALMACEN DISPONIBLE" sin tipo, id 4
    "REPARTO SAMU 1" COMPROMETIDA), `opciones_referencia/3` regresa solo
    el 4, con y sin sucursal activa. El `changeset/2` generado de
    Repartos (el mismo que usa la API) rechaza el almacén 2 con "el
    valor seleccionado no cumple los filtros configurados para este
    campo" y acepta el 4. Esa prueba corrió en una transacción revertida,
    sin residuo. **Pendiente:** confirmación visual en el navegador por
    el usuario.
33. [x] `mix precommit`: `compile --warning-as-errors` ✅; `test` 734/739.
    Los 5 fallos ya existían antes de este cambio (confirmado con
    `git stash` de `lib/`): 3 de `CaddyTest` y 1 de `MetaTepacheTest`
    por el entorno Windows (CRLF en el código fuente, `tar`), y 1 de
    `CatalogoLiveConsultaTest` (parámetro Fecha), que falla igual en HEAD.
    `credo.alcance`: 1 advertencia en `meta_importacion_datos.ex`,
    archivo no tocado. **El paso `format` NO se usó:** el repo no está
    formateado con el `line_length` por defecto, y `mix format`
    reescribe ~297 archivos (+27 mil líneas). Se revirtió, y los
    cambios de esta spec se reaplicaron sobre HEAD con el estilo de cada
    archivo. Se verificó que cada archivo reconstruido, formateado,
    queda idéntico a la versión formateada previa.
34. [x] `05.usage.md` §5.5 + ejemplo de Repartos;
    `docs/campos-filtros-en-cascada.md` actualizado.

## Nota — hallazgos reales registrados en `design.md` §7

No son tareas de esta spec (son comportamiento de `CatalogoGenerico`/
`MetaStateEngine`, fuera del tab Configuración en sí), pero se
originaron revisando este tab en la misma sesión, así que quedan
enlazados acá para no perder el rastro:

- Índice único de estado inicial sin filtrar `delete_guid` (corregido,
  migración `20260910193000_filtrar_borrados_en_indice_estado_inicial.exs`).
- `CatalogoGenerico.crear/2` ahora exige al menos un estado
  (`MetaStateEngine.estado_inicial/1`) para cualquier catálogo BC de
  negocio, no solo transición "alta" formal.
