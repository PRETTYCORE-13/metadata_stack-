# SPEC-ADN-2209202601 — Configuración de Sales Unit (app móvil)

**Documento:** Tasks · **Fase:** pendiente de ejecución.

Cada tarea es chica y verificable. Los catálogos siguen la skill
`patron-bc` (básico para el Grupo A, maestro-detalle para B-D) — acá
solo se listan los campos/reglas concretos de esta spec, no se repiten
los pasos mecánicos genéricos que la skill ya cubre.

## Grupo A — `pty_config` (Parámetros Base)

- [x] A1. Alta del BC básico `pty_config` (id 105): campos
  `pty_config_parametro` (string 100), `pty_config_parametro_tipo`
  (enum: boolean/integer/decimal/string/enum),
  `pty_config_parametro_valor` (string 100) — tabla física + migración
  + autómata Activo/Baja estándar generados contra Postgres real.
- [x] A2. **Hallazgo real, no solo supuesto** (verificado contra
  Postgres): el índice único que genera el motor es compuesto por los
  3 campos juntos — un segundo `parametro` con el mismo código pero
  `parametro_tipo`/`parametro_valor` distintos SE INSERTÓ sin
  problema. R2 no lo cubre el motor estándar solo (actualizado
  `02.design.md` §5.2). Regla PRE `parametro_codigo_unico` escrita,
  guardada y compilada en `pty_config` (vía `MetaReglasCodigo` — código
  vive en `meta_schema_reglas_codigo`, no en disco, patrón real
  post-rediseño 2026-07-21) — usa `MetaBcApi.listar/2` para consultar
  hermanos, nunca `CatalogoGenerico` directo desde la regla. Re-
  verificado el mismo caso: ahora se rechaza
  (`{:precondiciones, [%{mensaje: "ya existe otro parámetro con el
  código ..."}]}`), sin dejar residuo (rollback + conteo en 0).
- [x] A3. Verificado real: dar de baja un Parámetro con un valor ya
  cargado en un Perfil — el valor sigue en `pty_config_perfilesdet`
  sin tocarse. De paso quedó cubierto el caso más exigente (ver B4):
  un Parámetro SIN valor cargado nunca, dado de baja, deja de figurar
  como faltante en la completitud del Perfil (antes del fix de B4
  seguía contando, ver ahí).

## Grupo B — `pty_config_perfiles` + `pty_config_perfilesdet` (Perfiles)

**Hallazgo real que cambió este grupo, ver `02.design.md` §5.0**: un
catálogo detalle nunca corre reglas PRE al insertar un renglón nuevo
(solo al editar uno existente vía transición del maestro) — verificado
armando exactamente este caso. R6/R7 pasan de "regla PRE" a "índice
único de Postgres + función de escritura propia"
(`MetadataApp.Adn.ConfigSalesUnit`), decidido con el usuario.

- [x] B1. Alta del maestro-detalle: maestro `pty_config_perfiles` (id
  106, campo `pty_config_perfiles_descripcion`, autómata Activo/Baja
  estándar), detalle `pty_config_perfilesdet` (id 107, campos
  `pty_config_perfilesdet_parametro` referencia → `pty_config`,
  `pty_config_perfilesdet_parametro_valor` string 100) — generados
  contra Postgres real. **Sub-paso encontrado en el camino**: un
  catálogo detalle recién creado nace con la matriz de "Permisos de
  detalle" (Activo × este detalle) en deny-by-default — hubo que
  conceder insertar/actualizar/borrar a mano para poder probar nada.
- [x] B2/B3 → **reemplazadas** por `MetadataApp.Adn.ConfigSalesUnit.agregar_valor_perfil/3`
  (`lib/metadata_app/adn/config_sales_unit.ex`): valida el tipo contra
  `pty_config` (R7) y pre-chequea unicidad antes de insertar (R6),
  respaldado por la migración
  `20260922233000_indice_unico_parametro_por_perfil.exs` (índice
  parcial `(encabezado_id, pty_config_perfilesdet_parametro) WHERE
  delete_guid IS NULL`) como red de seguridad real ante una carrera.
  La regla PRE que se había escrito primero para `pty_config_perfilesdet`
  se dejó (no hace daño, cubre el camino de edición vía transición),
  pero NO es el mecanismo principal.
- [x] B4. Función `completitud_perfil/1` — construida, y corregida en
  el camino: "vivo" tiene que filtrar por estado Activo, no solo por
  `delete_guid` (un Parámetro dado de baja nunca toca `delete_guid`,
  solo cambia `estado_id` — sin el filtro extra seguía exigiéndose en
  la completitud). Verificado con el caso decisivo: Parámetro sin
  valor cargado nunca → incompleto; el mismo Parámetro dado de baja →
  completo (deja de contar).
- [x] B5. Verificado de punta a punta contra Postgres real, sin dejar
  residuo (limpieza por SQL directo al final, conteo en 0 en las 3
  tablas): valor válido (ok), valor de tipo incorrecto (rechazado),
  mismo parámetro duplicado en el mismo Perfil (rechazado), parámetro
  inexistente (rechazado).

## Grupo C — Eje Ventas: `pty_config_perfiles_salesu` + `_exc`

Mismo mecanismo real que Grupo B (§5.0) — índice único de Postgres
para R12/R14, función de escritura para R15 (`agregar_excepcion_ventas/3`,
ya escrita en `config_sales_unit.ex`), regla PRE solo donde corre de
verdad (maestro, R9).

- [x] C1. Alta del maestro-detalle: maestro `pty_config_perfiles_salesu`
  (id 108, campos `pty_config_perfiles_salesu_sales_unit` referencia →
  `meta_schema_sales_unit`, `pty_config_perfiles_salesu_perfil`
  referencia → `pty_config_perfiles`, autómata Activo/Baja), detalle
  `pty_config_perfiles_salesu_exc` (id 109, campos
  `pty_config_perfiles_salesu_exc_parametro` referencia → `pty_config`,
  `pty_config_perfiles_salesu_exc_parametro_valor` string 100) —
  generados contra Postgres real, permisos de detalle concedidos
  (Activo × `pty_config_perfiles_salesu_exc`).
- [x] C2. Migración `20260922234500_indices_unicos_eje_ventas.exs` —
  índice único parcial `(encabezado_id, pty_config_perfiles_salesu_exc_parametro)
  WHERE delete_guid IS NULL` en el detalle (R14).
- [x] C3. Misma migración — índice único parcial
  `(pty_config_perfiles_salesu_sales_unit) WHERE delete_guid IS NULL`
  en el maestro (R12).
- [x] C4. Regla PRE `perfil_sin_asignaciones_vivas` escrita, guardada y
  compilada en `pty_config_perfiles` (transición `baja`) — chequea
  ambos ejes por nombre de catálogo, con `catalogo_existe?/1` para que
  el chequeo del eje Reparto (Grupo D, todavía no existe) no rompa
  nada mientras tanto. Verificado real: rechaza la baja con una
  asignación Activa viva, permite la baja después de dar de baja esa
  asignación primero.
- [x] C5. `agregar_excepcion_ventas/3` verificada contra el catálogo
  real: valor válido (ok), tipo incorrecto (rechazado), duplicado
  (rechazado).
- [x] C6. `efectiva_ventas/1` construida y verificada con la cascada
  completa: sin asignación → puros defaults (R17); con asignación y
  Perfil parcialmente cargado → valores del Perfil + default para lo
  que falta; agregando una Excepción → esa gana por sobre el Perfil.
- [x] C7. **Hallazgo de paso, más benigno de lo esperado**: un segundo
  alta con la misma `sales_unit` (violando R12) SÍ vuelve
  `{:error, changeset}` limpio (no una excepción cruda como se temía
  en la sesión de Grupo B para el caso análogo) — el índice parcial
  alcanza sin necesitar una función de escritura extra para el
  maestro. Verificado de punta a punta sin dejar residuo (limpieza SQL
  final, conteo en 0 en las 5 tablas involucradas).

## Grupo D — Eje Reparto: `pty_config_perfiles_invlocation` + `_exc`

Mismas 7 tareas que el Grupo C (mecanismo real de §5.0: índice único +
`agregar_excepcion_reparto/3`, ya escrita), reemplazando `sales_unit`
por `inventory_location` (referencia → `meta_schema_inventory_location`)
y los nombres de catálogo/migración por su equivalente de reparto
(`pty_config_perfiles_invlocation(_exc)`, `efectiva_reparto/1`). Si
Grupo C ya cerró, D4 (equivalente a C4) completa el chequeo de los DOS
ejes en `perfil_sin_asignaciones_vivas` que había quedado parcial —
mismo recordatorio de C1 sobre permisos de detalle en deny-by-default.

- [x] D1-D7. Todo construido y verificado contra Postgres real, mismos
  4+3 casos que C5-C7 más la prueba decisiva de D4: Perfil con
  asignación de Ventas Y Reparto simultáneas — dar de baja solo una
  todavía rechaza la baja del Perfil (confirma que
  `perfil_sin_asignaciones_vivas` chequea los DOS ejes, no se conforma
  con el primero que encuentra libre); recién con las dos de baja la
  baja del Perfil funciona. Maestro `pty_config_perfiles_invlocation`
  id 110, detalle `..._exc` id 111, migración
  `20260923000000_indices_unicos_eje_reparto.exs`. Sin residuo (0 en
  las 7 tablas al final).

## Grupo E — Permisos

- [x] E1. Concedidos los 8 permisos (4 CRUD: leer/crear/editar/eliminar
  + 4 transiciones: alta/guardar/baja/reactivar) al rol `pty-dsd-admin`
  (id 14) sobre los 4 catálogos MAESTRO (`pty_config`,
  `pty_config_perfiles`, `pty_config_perfiles_salesu`,
  `pty_config_perfiles_invlocation`) — vía
  `Permissions.conceder_permisos_catalogo/2` (mismo contexto que usa
  `CatalogoPermisosLive`/"Conceder todos"). Los detalles (`..._det`/
  `..._exc`) no llevan permisos propios (R12 de la skill `patron-bc` —
  los da su maestro). Verificado por consulta directa: 32 filas
  (8 × 4) en `meta_schema_rol_permiso` para el rol 14.
- [ ] E2. **No verificado en esta sesión** — necesita un usuario real
  con sesión (login) para confirmar acceso/rechazo de punta a punta, y
  crear ese usuario/asignación de rol en la base compartida de dev
  arriesga dejar datos de prueba si algo falla a mitad de camino (ver
  `feedback_no_demo_data_en_dev_compartido`). Pendiente de verificar
  manualmente en el navegador: loguearse con un usuario que solo tenga
  `pty-dsd-admin` y confirmar que opera los 4 catálogos maestro, y que
  un usuario sin ese rol no puede.

## Grupo F — Publicación

- [ ] F1. **Corrección real encontrada acá**: `mix motor.publicar` ya
  NO es el mecanismo de "commit local acotado al catálogo" que
  describía la skill `patron-bc` (desactualizada, corregida aparte) —
  hoy dispara un DEPLOY real (`.github/workflows/bc-deploy.yml` →
  imagen Docker → servidor por SSH), exige `--sistema=<sistema>` (un
  cliente real de `priv/sistemas.json`, o `"unstable"` para probar) y
  nunca toca `origin/main` (cero `git add`/commit/push en todo el
  flujo). **Decisión explícita del usuario: no ejecutarlo desde acá —
  el despliegue de estos 4 catálogos queda a su cargo.** Los 4
  catálogos quedan completos y verificados en el Postgres de dev
  local, listos para publicar cuando el usuario lo decida
  (`mix motor.publicar --sistema=<...> pty_config pty_config_perfiles
  pty_config_perfiles_salesu pty_config_perfiles_invlocation`, en ese
  orden o dejando que `calcular_paquete_publicacion/1` resuelva el
  orden topológico solo).
- [x] F2. `05.usage.md` escrito (ver archivo) una vez que A-E cerraron
  — cubre el flujo real de uso vía Motor BC + las funciones de
  `ConfigSalesUnit`, con nota explícita de que la publicación (F1)
  queda pendiente y a cargo del usuario.
