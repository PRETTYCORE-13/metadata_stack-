# Tasks — Seguridad real en la API genérica (`/api`)

Cada tarea es chica y verificable por sí sola. Se implementa con "seguí
`03.tasks.md`, tarea N".

## Grupo A — `RequierePermiso`: acción también dinámica (R6, design.md §2)

- [x] **A1.** `accion = Keyword.get(opts, :accion) || conn.params["accion"] ||
      raise(...)` en `plugs/requiere_permiso.ex`. Verificable: `mix
      compile` limpio + los 3 controllers que ya usan el plug con
      `accion:` fija (`RolController`, `PermisoController`,
      `UsuarioRolController`) siguen compilando y su comportamiento no
      cambia (mismo `Keyword.get` con fallback, nunca afecta cuando
      `accion:` ya viene fija).

## Grupo B — Conectar el plug (R1-R8, design.md §3)

- [x] **B1.** `CatalogoController` — 4 líneas de `plug` (leer/crear/
      editar/eliminar, recurso dinámico). Verificable: `mix compile`
      limpio.

- [x] **B2.** `MetaTransicionController` — 2 líneas (`:index` fija
      "leer", `:ejecutar` sin `accion:` fija). Verificable: `mix
      compile` limpio.

- [x] **B3.** `MetaSchemaHeaderController` — 2 líneas (`sysadmin_bc`
      leer/editar). Verificable: `mix compile` limpio.

- [x] **B4.** `MetaEstadoController` — 2 líneas (`sysadmin_bc` leer en
      `:index`, editar en `:create`). Verificable: `mix compile`
      limpio.

- [x] **B5.** `MetaTransicionAdminController` — mismo patrón que B4.
      Verificable: `mix compile` limpio.

- [x] **B6.** `CatalogoAdminController` — 2 líneas (`sysadmin_bc` leer
      en impacto/validar_motor/completitud, editar en `:delete`).
      Verificable: `mix compile` limpio.

## Grupo C — Verificación real, por cada controller (request tests contra Postgres real)

Mismo patrón en los 6: sin sesión → 401; con sesión pero sin el permiso
→ 403; con sesión y el permiso correcto → pasa normal; usuario con rol
"administrador" → pasa siempre (R9).

- [x] **C1.** `CatalogoController` — las 4 acciones × los 3 casos
      (401/403/200), sobre un catálogo fixture real con permisos
      concedidos/no concedidos a propósito.

- [x] **C2.** `MetaTransicionController` — `:index` (401/403/200) y
      `:ejecutar` confirmando que el permiso chequeado es el de LA
      transición pedida (ej. permiso para "alta" no alcanza para
      ejecutar "baja").

- [x] **C3.** `MetaSchemaHeaderController`, `MetaEstadoController`,
      `MetaTransicionAdminController`, `CatalogoAdminController` —
      401/403/200 contra `sysadmin_bc`, un solo round por grupo ya que
      comparten el mismo recurso fijo.

- [x] **C4.** Regresión explícita: `RolController`/`PermisoController`/
      `UsuarioRolController` (ya protegidos antes de esta spec) siguen
      con el mismo comportamiento exacto (ningún test existente de esos
      3 se rompe).

- [x] **C5.** Suite completa (`mix test`) sin regresiones.

## Grupo D — Verificación end-to-end real (fuera de test, contra Postgres real)

- [x] **D1.** Contra `dev` local: un request sin `Authorization`/sin
      cookie de sesión a `GET /api/pty_h_historico` (o cualquier
      catálogo real) responde `401`, no `200` con datos.

- [ ] **D2.** Deploy a `unstable` (esto es código de plataforma, no
      config de catálogo — llega por el camino normal, commit + push a
      `main`, CI despliega solo). Confirmar en `unstable` que un
      request sin sesión a la API genérica también da `401` ahí.
